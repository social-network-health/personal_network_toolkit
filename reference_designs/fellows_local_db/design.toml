# Reference-design manifest for fellows_local_db. See
# reference_designs/templates/design.toml for the field reference.
#
# This design is "accepted by definition" (the toolkit was distilled from it).
# Software Heritage archival was requested 2026-06-08 (Save Code Now request
# 2352383) at the post-MVD commit below, and the git-compatible SWHIDs are
# recorded now. archival stays "pending" until SH ingest completes (async) AND a
# [verify] entrypoint lands — the latter awaits a schema-shaped evaluate-report.json
# (docs/roadmap.md Tier 0). The lint permits a pending design to carry valid
# SWHIDs; flip to "archived" once both conditions are met.

name = "fellows_local_db"
repo = "https://github.com/richbodo/fellows_local_db"
toolkit_version = "0.1"
status = "active"
archival = "pending"   # → "archived" once SH ingest completes AND a [verify] entrypoint lands

commit    = "bbaf66e874ed3b97d0ce5f01f468f9e8fc4193e5"
swhid_rev = "swh:1:rev:bbaf66e874ed3b97d0ce5f01f468f9e8fc4193e5"
swhid_dir = "swh:1:dir:a52fa259b71d0b42261a5f3926fffb66a2d0c4ab"

[flavor]
distribution = "web-bundle-with-magic-link"
storage = "opfs-sqlite-wasm"
ingestion = "single-source-static-mirror"
workspace = "vanilla-js-spa"
comms = "mailto-only"
mcp-exposure = "shared+private+comms"

[verify]
runner = "container"
entrypoint = ""    # TODO(maintainer): command that builds + runs the e2e/conformance suite
emits_report = "evaluate-report.json"
